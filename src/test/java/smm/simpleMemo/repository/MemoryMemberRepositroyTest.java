package smm.simpleMemo.repository;

class MemoryMemberRepositroyTest {
/*
    @Autowired
    MemberRepositroy resp;

    @AfterEach
    public void afterEach() {
        resp.clearStore();
    }

    @Test
    public void save() {
        Member member = new Member();
        member.setName("Spring");

        resp.save(member);

        Member result = resp.findById(member.getId()).get();
        Assertions.assertThat(member).isEqualTo(result);
        //Assertions.assertEquals(result, member);
        //System.out.println("result = " + (result == member));

    }

    @Test
    public void findByName() {
        Member member1 = new Member();
        member1.setName("Spring1");
        resp.save(member1);

        Member member2 = new Member();
        member2.setName("Spring2");
        resp.save(member2);

        Member result = resp.findByName("Spring2").get();
        Assertions.assertThat(result).isEqualTo(member2);
    }

    @Test
    public void findAll() {
        Member member1 = new Member();
        member1.setName("Spring3");
        resp.save(member1);

        Member member2 = new Member();
        member2.setName("Spring4");
        resp.save(member2);

        List<Member> result = resp.findAll();
        Assertions.assertThat(result.size()).isEqualTo(2);
    }
*/
}
